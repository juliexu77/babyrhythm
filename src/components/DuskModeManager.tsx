/**
 * DuskModeManager - Previously auto-switched themes based on time of day.
 * Now disabled - users have full manual control via ThemeToggle.
 */
export function DuskModeManager() {
  // Auto-switching disabled - theme is now fully user-controlled
  return null;
}
